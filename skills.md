# skills.md — دستور الهندسة لخادم flaminGO Backend

**الحالة:** مرجع تشغيلي دائم وملزم لأي تعديل أو تطوير على هذا المستودع (NestJS + Prisma + PostgreSQL + Redis).
**النطاق:** كل من يعمل على هذا الكود — أنا، أو أي نموذج ذكاء اصطناعي آخر يُستدعى لاحقًا — يلتزم بهذه القواعد حرفيًا قبل كتابة أي سطر كود.

هذا الملف ليس توصيات عامة. كل قاعدة هنا مبنية على قراءة فعلية لكود موجود بالفعل في المستودع، لا على افتراضات معمارية نظرية.

---

## 0. لماذا هذا الملف موجود

النظام الحالي ناضج هندسيًا في نقاط حرجة (الدفتر المالي، التخصيص الذرّي للسائقين، تقسيم جدول التتبع). أكبر خطر على هذا النضج ليس نقص الميزات — بل **تعديل عشوائي يكسر ضمانًا موجودًا دون أن يظهر الكسر فورًا** (مثل: كسر التوازن المحاسبي، أو فتح نافذة تخصيص مزدوج، أو فقدان حدث صامتًا). هذا الملف موجود لمنع ذلك تحديدًا.

---

## 1. قاعدة العمل الأساسية: الواقعية الصارمة وصفر تكهّن (Zero Hallucination)

### 1.1 القاعدة المطلقة
- **ممنوع منعًا باتًا** اقتراح أو استخدام مكتبة، دالة، حقل Prisma، أو نمط معماري غير موجود فعليًا في المستودع — إلا إذا كان التعديل المطلوب صراحة هو **إضافة** تلك المكتبة/الدالة، وعندها يجب ذكر ذلك بوضوح كإضافة جديدة، لا كأمر واقع.
- **ممنوع** افتراض وجود بنية تحتية غير مؤكدة من الكود (مثال: لا يوجد Kafka، لا يوجد BullMQ، لا يوجد PostGIS، لا يوجد Redis Cluster/Sentinel في `docker-compose.yml` الحالي — أي اقتراح يتضمنها هو **اقتراح إدخال جديد** يجب تصنيفه كذلك صراحة، لا كتعديل على شيء قائم).
- **ممنوع** كتابة كود يتوهّم توقيع (signature) دالة موجودة. قبل استدعاء أي دالة من خدمة قائمة (`LedgerCoreService.post`, `DistributedLockService.withLock`, `OutboxService.enqueue`...) يجب التحقق من توقيعها الفعلي في الملف المصدر أولًا، لا من الذاكرة.

### 1.2 البناء فوق الموجود لا استبداله
كل تعديل يُبنى فوق الأنماط القائمة التالية بالاسم — وهي ثابتة، لا تُستبدل إلا بقرار معماري صريح موثّق:
- التحديث الشرطي الذرّي (`updateMany` بحارس حالة) كآلية منع التخصيص المزدوج — كما في `assignDriver` (`driver.updateMany({ where: { id, availability: "ONLINE" } , ... })`) و`reapStuckSearchesTask`.
- جسر `CH_OFFER_RESPONSE` (Redis Pub/Sub مخصص لإيصال رد السائق عبر النسخ) — نمط مقبول للحالات التي **يُصحِّح المهلة الزمنية نفسها فقدان الرسالة تلقائيًا** (timeout = شبكة أمان). لا يُستنسخ هذا النمط لأي حالة لا يوجد فيها تصحيح ذاتي عند الفقدان.
- تصميم `LedgerCoreService` (القيد المزدوج، `idempotencyKey`، `balanceAfter`، `Serializable` عند العكس).
- الفصل بين `enqueue()` (الذرية مع معاملة العمل) و`relayDueEventsTask` (التسليم الفعلي) في نمط Outbox — هذا الفصل بنيوي ومقصود، لا يُدمَج مرة أخرى.

### 1.3 عند عدم اليقين
إن لم يكن الملف المصدري قد قُرئ فعليًا في هذه الجلسة أو الجلسة الحالية لا تملك تأكيدًا حديثًا لمحتواه، الإجراء الإلزامي هو **قراءة الملف أولًا**، لا افتراض محتواه من نمط تسمية أو من فهم سابق قد يكون تغيّر.

---

## 2. قواعد النواة المالية والمحاسبية (Ledger Rules)

### 2.1 خطوط حمراء غير قابلة للتفاوض
| الممنوع | لماذا |
| --- | --- |
| أي قيد يتجاوز `LedgerCoreService.assertBalanced` أو يُدخل بمجموع مدين ≠ دائن | يكسر مبدأ القيد المزدوج نفسه — كل الدفتر يفقد معناه المحاسبي |
| أي ترحيل مالي حرج يتم **خارج** `Prisma.TransactionIsolationLevel.Serializable` عند العكس (`reverseTransaction`)، أو خارج معاملة `tx` عادية عند الترحيل الأول (`post`) | يفتح نافذة سباق بين قراءة الرصيد وكتابته |
| تكرار استدعاء `post()` بنفس المنطق دون `idempotencyKey` فريد وواضح المصدر | يُبطل الحماية الوحيدة ضد الترحيل المزدوج عند إعادة المحاولة (retry) |
| أي نداء HTTP خارجي (مزوّد دفع، بوابة SMS، إلخ) **داخل** نفس معاملة Postgres التي تُرحّل القيود | يطيل قفل صفوف الحسابات المالية بمقدار زمن استجابة خدمة خارجية — خطر مباشر تحت الحمل |
| تعديل `LedgerEntry` أو `LedgerTransaction` بعد `status: POSTED` مباشرة عبر `update` | السجل المالي المُرحَّل غير قابل للتعديل بتصميم النظام — التصحيح الوحيد المسموح هو قيد عكسي جديد عبر `reverseTransaction` |

### 2.2 آلية تقسيم حساب المنصة (Sharding) — الالتزام الإلزامي عند التنفيذ

عند تنفيذ تقسيم `PLATFORM:COMMISSION:{currency}` (أو أي حساب منصة آخر يتعرّض لتزاحم عالٍ)، يُلتزم بما يلي حرفيًا:

1. **مفتاح الشظية**: `shardIndex = hash(tripId) % N` حيث `N` عدد الشظايا (تُضبط عبر متغيّر بيئة، لا رقم مثبَّت في الكود). الشرط: **نفس الرحلة تذهب دائمًا لنفس الشظية** في كل نداء (لا عشوائية) — هذا شرط لازم لأي تسوية/عكس لاحق لنفس الرحلة أن يجد نفس الحساب.
2. **صيغة الكود**: `PLATFORM:COMMISSION:{currency}:shard_{NN}` بنفس نمط `platformAccount()` الحالي في `LedgerCoreService` — **لا يُنشأ مسار إنشاء حساب جديد موازٍ**؛ يُستدعى `platformAccount()` نفسها بـ `codeSuffix` محسوب ديناميكيًا، لا تُعاد كتابتها.
3. **التوافقية مع `reconciliation.util.ts`**: أي دالة تجميع لرصيد المنصة الكلي (لوحة تحكم، تقرير) يجب أن تمر عبر دالة توفيق جديدة تُضاف إلى `reconciliation.util.ts` (وليس مبعثرة في أماكن أخرى) تجمع `SUM(balanceCache)` عبر كل الشظايا لنفس بادئة الحساب، مع اختبار مطابق في `reconciliation.util.spec.ts` يغطي: (أ) تطابق مجموع الشظايا مع مجموع `LedgerEntry` الفعلي، (ب) سلوك التجميع عند تغيّر `N` (إعادة تقسيم مستقبلية).
4. **قاعدة عدم الكسر**: أي كود قائم يقرأ `PLATFORM:COMMISSION:{currency}` **بصيغته غير المُقسَّمة** (بدون `shard_`) يجب أن يستمر بالعمل خلال فترة انتقالية — إما عبر حساب تجميعي للقراءة فقط يُحدَّث من الشظايا، أو عبر تعديل صريح لكل نقطة استدعاء موثَّقة بقائمة كاملة قبل الدمج، لا تعديل جزئي يترك بعض القراءات على الحساب القديم وبعضها على الشظايا.

---

## 3. قواعد التزامن والأقفال الموزّعة (Concurrency & Distributed Locking)

### 3.1 التراتبية الثابتة للدفاع — لا تُعكَس أبدًا
```
خط الدفاع الأول (اختياري، تخفيف تزاحم فقط):
    redis.acquireLock / DistributedLockService.acquire
    → فشله لا يجوز أن يمنع تنفيذ العملية (fail-open حيث يُطبَّق فعليًا، كما في offerToBatch)

خط الدفاع الأخير (إلزامي، لا بديل عنه):
    معاملة قاعدة بيانات ذرّية شرطية (updateMany بحارس حالة + تحقق داخل نفس tx)
    → هذا وحده يقرر النتيجة النهائية بشكل لا لبس فيه
```
**أي كود جديد يعتمد على القفل الموزّع وحده كضمان نهائي ضد التعارض مرفوض بالتصميم.** يجب أن يكون خلفه دائمًا معاملة DB شرطية على غرار `assignDriver`.

### 3.2 الانتقال من Redis عقدة واحدة إلى Redlock حقيقي

الالتزام عند التنفيذ:
1. **لا تغيير في الواجهة العامة**: `DistributedLockService.acquire(name, options)`, `.release(name, token)`, `.withLock(name, fn, options)`, `.runExclusive(name, fn, ttlMs)` تبقى بنفس التوقيع والسلوك الظاهري تمامًا. كل مستدعٍ حالي (`OutboxService`, `matching.service.ts` عبر `cronLock`, أي كود مستقبلي) لا يُعدَّل.
2. **التغيير محصور داخليًا**: فقط تفاصيل التنفيذ داخل `acquire`/`release` تتغيّر — من عميل Redis واحد (`this.redis.client.set(...)`) إلى منطق أغلبية عبر N عُقد Redis مستقلة (عادة 5، عبر مكتبة `redlock` الرسمية أو تطبيق مكافئ). العقد يجب أن تكون **مستقلة فعليًا** (لا تتشارك بيانات) — Sentinel/Cluster عادي لا يحقق ضمان Redlock لأنه لا يزال نظام تصويت واحد منطقيًا عند فشل الشبكة.
3. **شرط القبول**: أي تنفيذ لـ Redlock يجب أن يمرّ باختبار محاكاة فشل عقدة واحدة من N (يبقى القفل صحيحًا طالما توفرت الأغلبية) — لا يُعتمد التنفيذ دون هذا الاختبار.
4. **`runExclusive` تحديدًا** (مهام cron): يبقى سلوكه "محاولة واحدة بلا انتظار، تخطَّ إن فشلت" — هذا **لا يتغيّر** حتى مع Redlock، لأن طبيعة الاستخدام (تخطي مهمة مجدولة إن كانت نسخة أخرى تنفّذها) لا علاقة لها بعدد عقد الأغلبية.

---

## 4. قواعد تدفّق الأحداث والصندوق الصادر (Outbox & Event Streaming)

### 4.1 ما لا يتغيّر أبدًا
- توقيع `OutboxService.enqueue(tx, name, payload, options)` **يبقى كما هو حرفيًا**. كل كود نطاق تجاري (`settleTrip`, `assignDriver` المستقبلي، أي عملية جديدة) يستمر باستدعائه ضمن نفس معاملة العمل دون أي تعديل.
- بنية جدول `OutboxEvent` (الحالات، `dedupeKey`، `attempts`/`maxAttempts`) ومنطق `nextOutboxState`/`OUTBOX_MAX_ATTEMPTS` في `outbox.util.ts` تبقى المرجع الوحيد لحالة إعادة المحاولة وDLQ — لا تُعاد كتابتها عند تغيير طبقة النقل.

### 4.2 بروتوكول الانتقال من Redis Pub/Sub إلى Kafka/NATS JetStream

**نقطة التغيير الوحيدة المسموحة**: داخل `OutboxService.relayDueEventsTask()` (أو `dispatch()`) — استبدال `this.events.emit(event.name, event.payload)` بنداء إنتاج (`producer.send`) إلى الناقل الجديد، مع الحفاظ على:
1. **مفتاح التقسيم (partition key)**: يجب أن يكون معرّفًا ثابتًا من الحدث (مثال: `tripId` أو `referenceId`) لضمان **ترتيب الأحداث لنفس الكيان** — شرط لازم لأن بعض الأحداث المالية/رحلة تعتمد ترتيبها (لا يجوز أن يصل حدث "اكتملت الرحلة" قبل "بدأت الرحلة" لنفس `tripId`).
2. **الاسم الطوبولوجي (`topic`/`subject`) = `event.name` مباشرة** — لا تُعاد تسمية الأحداث الموجودة (`catalog.category.updated` ونظائرها)؛ أي مستهلك حالي (Handler مسجَّل عبر `EventBusService.on()`) يجب أن يستمر بالعمل دون تعديل حتى نهاية فترة الانتقال، عبر جسر مؤقت يعيد نشر الحدث محليًا أيضًا (fan-out مزدوج: Kafka + `EventEmitter` المحلي) إلى أن يُهاجَر كل مستهلك صراحة.
3. **إمكانية إعادة التشغيل (Replay) إلزامية بالتصميم الجديد**: أي مستهلك جديد يُبنى بافتراض **at-least-once delivery** ويجب أن يكون idempotent بذاته (تمامًا كما تُوثِّق `OutboxService` نفسها: "التسليم at-least-once: يجب أن يكون المستهلكون idempotent") — لا يُعتمد أي مستهلك جديد يفترض تسليمًا مضمونًا لمرة واحدة فقط.
4. **لا حذف لآلية DLQ الحالية**: عند فشل التسليم النهائي بعد `maxAttempts`، السلوك (`status: DEAD` + تسجيل في `outboxEvent`) يبقى كما هو — طبقة النقل الجديدة لا تستبدل جدول `OutboxEvent` بأي طابور Dead-Letter خاص بها من طرف واحد؛ حالة الحدث في PostgreSQL تبقى مصدر الحقيقة الوحيد لواجهة DLQ الحالية (`listDeadLetters`, `retryDeadLetter`).
5. **قبول التنفيذ مشروط باختبار فقدان اتصال**: أي تكامل جديد مع Kafka/NATS يجب اختباره تحت سيناريو انقطاع اتصال الناقل لفترة (محاكاة) والتأكد أن الأحداث تبقى `PENDING`/`FAILED` في `OutboxEvent` (لا تُفقَد، ولا تُعلَّم كـ`DELIVERED` كذبًا) — هذا هو الفرق الجوهري المطلوب تحقيقه فعليًا عن حالة Redis Pub/Sub الحالية.

### 4.3 الجسر الخاص (`CH_OFFER_RESPONSE`) لا يدخل ضمن هذا الانتقال
جسر رد عرض السائق في `matching.service.ts` **ليس** جزءًا من نمط Outbox ولا يُهاجَر إلى Kafka ضمن هذه المرحلة — طبيعته (رسالة قصيرة العمر، تصحيح ذاتي بالمهلة الزمنية، زمن استجابة يجب أن يبقى بالميلي ثانية) تناسب Redis Pub/Sub تحديدًا. أي اقتراح لنقله إلى ناقل رسائل دائم يجب أن يُبرَّر صراحة بمشكلة قياسها فعليًا، لا كتعميم تلقائي لقاعدة القسم 4.2.

---

## 5. بروتوكول تنفيذ الكود (Implementation Protocol)

عند تنفيذ أي تعديل مستقبلي، يُلتزم بالشكل التالي دون استثناء:

### 5.1 قبل الكتابة
- تحديد **الملف المستهدف بالمسار الكامل** (مثال: `src/modules/financial/ledger-core.service.ts`) — لا تعديلات "في مكان ما في النظام المالي".
- قراءة الملف الفعلي (أو تأكيد قراءته حديثًا في نفس الجلسة) قبل اقتراح أي تعديل عليه.

### 5.2 عند التسليم
كل رد يتضمن تعديلًا برمجيًا يجب أن يحوي:
1. **الكود كاملًا وصحيحًا** بلغة TypeScript/NestJS/Prisma حسب السياق — لا مقتطفات ناقصة تفترض أجزاءً "بديهية"، ولا `// ... باقي الكود كما هو` في مواضع تغيّر السلوك.
2. **اسم الملف المستهدف** صريحًا في أعلى كل كتلة كود.
3. **قسم "الأثر الجانبي" (Side Effects)** إلزامي بعد كل تعديل، يجيب تحديدًا عن:
   - أي وحدات/خدمات أخرى تستدعي هذا الكود المعدَّل (بحث فعلي عن نقاط الاستدعاء، لا تخمين)؟
   - هل يتغيّر توقيع أي دالة عامة (public method)؟ إن كان كذلك، سرد كل نقطة استدعاء تحتاج تعديلًا مرافقًا.
   - هل يتطلب التعديل ترحيل Prisma (`migration`)؟ إن كان كذلك، هل هو متوافق مع الخلف (backward-compatible) أثناء نشر تدريجي (rolling deploy) حيث تعمل نسخ قديمة وجديدة من الكود على نفس القاعدة لحظيًا؟
   - هل يؤثر على مسار حرج مذكور في هذا الملف (الدفتر المالي، التخصيص الذرّي، Outbox)؟ إن كان كذلك، أي قاعدة من الأقسام 2-4 أعلاه تنطبق تحديدًا؟

### 5.3 عند الشك في التوافق مع قاعدة قائمة
إن بدا أن التنفيذ المطلوب يتعارض مع قاعدة في هذا الملف (مثال: طلب تعديل يكسر الذرية بين `enqueue` ومعاملة العمل)، **يُصرَّح بالتعارض صراحة قبل الكتابة** ويُقترح بديل يحافظ على القاعدة — لا يُنفَّذ التعارض بصمت حتى لو كان الطلب مباشرًا.

---

## سجل الاعتماد

| البند | الحالة |
| --- | --- |
| تاريخ الإعداد | يُملأ عند الاعتماد النهائي |
| مبني على فحص فعلي لملفات | `ledger-core.service.ts`, `outbox.service.ts`, `event-bus.service.ts`, `distributed-lock.service.ts`, `matching.service.ts` (كامل), `redis.service.ts`, `main.ts`, `docker-compose.yml`, `schema.prisma` (النماذج المالية وTrip/Driver/TripTracking/OutboxEvent), ملف ترحيل `20260825122500_trip_tracking_partitioning` |
| نطاق الإلزام | كل تعديل برمجي لاحق على هذا المستودع، من أي مساعد ذكاء اصطناعي |

# flaminGO Backend — التقرير الشامل لحالة الخادم

**تاريخ التقرير:** 27 جويلية 2026
**اسم الحزمة:** `nova-backend` (NOVA Ride — تحت علامة flaminGO)
**نطاق التقرير:** كل ما عُدِّل من المرحلة 0 إلى المرحلة 7 (الجزء السادس)، وحالة الخادم الحالية، ومدى جاهزيته للانطلاق.

---

## 1. الخلاصة التنفيذية (اقرأ هذا إن لم تقرأ غيره)

| السؤال | الجواب المباشر |
| --- | --- |
| هل الخادم مكتمل مثل خوادم التطبيقات العالمية؟ | **من ناحية المنطق والوظائف: نعم، ~90%.** من ناحية التشغيل (Ops) والاختبارات والمراقبة: **لا، ~55%.** |
| هل هو جاهز للبدء؟ | **جاهز لإطلاق مُتحكَّم فيه** (مدينة واحدة، عدد محدود من السائقين) — **وليس** جاهزًا لإطلاق وطني واسع اليوم. |
| ما الذي يمنع الإطلاق الآن؟ | خطوات تشغيلية بيدك أنت لا بيد الكود: تدوير المفاتيح، تشغيل محرّك المسارات OSRM، تنفيذ ترحيلات قاعدة البيانات، بيانات Firebase، بناء المشروع. |
| ما أخطر ما تبقّى تقنيًا؟ | تغطية اختبارات منخفضة (~35%) ومراقبة ضعيفة (~45%) — أي أنّنا نعمل بلا شبكة أمان تكشف الانحدار قبل المستخدم. |
| التقييم العام قبل التعديلات | **71 / 100** |
| التقييم العام بعد التعديلات (تقدير) | **86 / 100** |

---

## 2. من أين بدأنا: التشخيص الأصلي

راجعنا الخادم كاملًا (~455 ملف TypeScript، ~45 ألف سطر، 60 وحدة، ~90 متحكّمًا، ~476 نقطة نهاية، 100 جدول في قاعدة البيانات). وكانت النتيجة **71/100** مع مشكلتين قاتلتين:

1. **لا محرّك مسارات حقيقي.** المسافة كانت تُحسب بخط مستقيم (Haversine) والزمن بسرعة ثابتة 28 كم/سا. النتيجة: تسعير خاطئ في مدينة مثل الجزائر العاصمة (نهر، جسور، اتجاه واحد)، ووقت وصول متوقّع لا يشبه الواقع. هذا وحده كافٍ لقتل ثقة المستخدم.
2. **حرّاس الحماية لم تكن عالمية.** أي نقطة نهاية يُنسى فيها الديكوريتور كانت مفتوحة للعالم.

وكانت هناك عائلة أخطر من الأخطاء أسمّيها **«النجاح الكاذب»**: كود يُرجع `success: true` بينما لم يحدث شيء فعليًا — رمز OTP «أُرسل» بلا SMS، دفع «تم تحصيله» بلا حركة مال، نداء استغاثة SOS يُخزَّن ولا يُبلَّغ أحد. هذا النوع أخطر من الانهيار الصريح، لأن الانهيار يُرى والنجاح الكاذب يُخفي الضرر.

---

## 3. كل التعديلات مرحلة بمرحلة

### المرحلة 0 — إحكام الأمن

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| ثلاثة حرّاس عالميين مرتّبين: توثيق ← أدوار ← صلاحيات | القاعدة انقلبت: كل شيء مغلق افتراضيًا، والانفتاح استثناء صريح بـ `@Public()` |
| ديكوريتور `@Public()` على النقاط العامة فقط | نسيان الديكوريتور صار يعني «مغلق» لا «مفتوح» — الخطأ الآمن |
| التحقق من توقيع HMAC على webhooks الدفع + التقاط الجسم الخام | بدون الجسم الخام لا يمكن التحقق من التوقيع؛ وبدون توقيع يستطيع أي أحد تزوير «تم الدفع» |
| حذف ملف `.env` من المستودع | الأسرار كانت مكشوفة — **يجب تدويرها كلها يدويًا** |

### المرحلة 1 — محرّك المسارات الحقيقي

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| مزوّد OSRM + طبقة `RoutingService` مع تخزين مؤقت في Redis | مسافة وزمن حقيقيان على الطريق لا خط مستقيم |
| ربط التسعير والمطابقة بالمحرّك (استراتيجية «أسرع وصولًا») | السائق الأقرب بالخط المستقيم قد يكون الأبعد زمنيًا (جسر، نهر) — الآن نختار الأسرع فعلًا |
| تخزين `routePolyline` و`routeProvider` على الرحلة | نحفظ المسار الذي وعدنا به لنكشف الانحراف لاحقًا ولنحلّ الشكاوى بدليل |

### المرحلة 2 — مزوّدون حقيقيون بدل النجاح الكاذب

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| طبقة `PaymentAdapter` مجرّدة (نقد، محفظة، ثم البطاقات) | تغيير مزوّد الدفع يصير تبديل قطعة لا إعادة كتابة |
| OTP عبر Firebase Phone Auth حصرًا + التحقق من الادعاءات الموقّعة فقط | كان الكود يثق برقم الهاتف القادم من العميل — أي انتحال هوية مباشر. الآن لا نثق إلا بتوقيع Firebase |

### المرحلة 3 — فصل العمليات الخلفية وأقفال المهام

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| قفل موزّع `runExclusive()` على 13 مهمة مجدولة | مع نسختين من الخادم كانت كل مهمة تعمل مرتين: صرف مضاعف، إشعار مكرّر |
| دور تشغيلي `APP_ROLE=api` أو `worker` | حركة الركّاب لا تتأثر بمهمة تقارير ثقيلة، وكل دور يتوسّع منفصلًا |

### المرحلة 4 و5 — الميزات التي تفصل تطبيق نقل عن «تطبيق يمشي»

| الميزة | لماذا نحتاجها |
| --- | --- |
| مشاركة الرحلة برابط مؤقت + توصيل نداء SOS فعليًا | نداء الاستغاثة كان يُحفَظ ولا يُبلَّغ أحدًا. الأمان ليس ميزة تسويقية بل شرط بقاء |
| تسعير الذروة الحيّ (Surge) بخلايا جغرافية | كانت دالة الذروة مكتوبة ولا يناديها أحد — العرض والطلب لم يكن يؤثّر في السعر |
| كشف الانحراف عن المسار (600 م، 3 مرات) | يقارن GPS الحقيقي بالمسار المحفوظ: أمان الراكب + كشف التطويل المتعمّد |
| إخفاء أرقام الهواتف (طبقة مجرّدة) | لم نعد نكشف رقم الراكب للسائق ولا العكس |
| بلاغات المفقودات | كان الغياب كاملًا: الراكب ينسى هاتفه ولا مسار رسمي |
| فواتير PDF حقيقية مخزَّنة | `pdfkit` كان مثبَّتًا وغير مستخدم؛ ومن دون فاتورة لا عملاء شركات |
| تقسيم جدول التتبّع الجغرافي شهريًا + حذف تلقائي | الجدول كان ينمو بلا حد؛ أضخم جدول في أي تطبيق نقل وهو من يقتل الأداء أولًا |

### المرحلة 6 — اكتمال الحلقة المالية

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| تخزين إعدادات مؤقت بطبقتين (ذاكرة + Redis) مع إبطال فوري | كل تغيير راية ميزة كان يضرب قاعدة البيانات في كل طلب |
| إصدار الفاتورة تلقائيًا بعد كل رحلة | لا يُطلب من أحد إصدارها يدويًا |
| إكرامية السائق (نافذة 72 سا، حدود، قيم مقترحة) | مصدر دخل مباشر للسائق بلا تكلفة على المنصة |
| توصيل الولاء والإحالات بالرحلات فعليًا | الخدمتان كانتا موجودتين ولا ينادِيهما شيء — نقاط لا تُمنح، ومُحيل لا يُكافأ |
| مسار صرف أرباح السائقين (دفعات + IBAN + انتقالات حالة صارمة) | كانت الأرباح تُحسب ولا تخرج. السائق الذي لا يُقبض لا يعود |

### المرحلة 7 — الطبقة الاحترافية النهائية

| ما عُمل | لماذا نحتاجه |
| --- | --- |
| إخفاء أرقام حقيقي عبر Twilio + وضع `direct` للاتصال المباشر مؤقتًا | تبدأ اليوم بالاتصال المباشر، وتشتري أرقام Twilio لاحقًا بتغيير متغيّر بيئة واحد |
| مُحوّل Chargily Pay v2 حقيقي (تحقق توقيع، مهلة، خرائط حالة) | الدفع بالبطاقة صار جاهزًا للتشغيل لحظة وصول مفاتيحك |
| مزوّد بريد + 6 قوالب موحّدة بثلاث لغات (ar/fr/en) مع RTL | رسالة واحدة متسقة بهوية flaminGO بدل نصوص متفرقة |
| أرشفة الرحلات القديمة إلى نسخة باردة | جدول الرحلات ينمو للأبد؛ الأرشفة تحمي سرعة الاستعلامات بعد سنة |
| توصيل البريد بأحداث حقيقية (فاتورة، إيصال، صرف أرباح، مفقودات) | القوالب كانت تُبنى ولا تُرسَل من أي حدث أعمالي |

---

## 4. شكل الخادم حاليًا

### الطبقات

```
العميل (تطبيق الراكب / السائق / لوحة الإدارة)
        │  HTTPS + WebSocket
        ▼
طبقة الحماية: JwtAuthGuard → RolesGuard → PermissionsGuard  (عالمية)
        ▼
60 وحدة NestJS  ─ رحلات · مطابقة · تسعير · مالية · محفظة · مدفوعات ·
                   صرف أرباح · إشعارات · بريد · أمان · مفقودات · فواتير ·
                   إكراميات · ولاء · إحالات · اتصالات مقنّعة · أرشفة
        ▼
البنية التحتية: PostgreSQL (Prisma) · Redis (موقع/أقفال/تخزين مؤقت) ·
                 OSRM (مسارات) · Firebase (OTP + إشعارات) ·
                 Google Cloud Storage (فواتير/ملفات) · Chargily (بطاقات)
```

### دور API ودور Worker

- **api**: يخدم الطلبات و WebSocket فقط.
- **worker**: يشغّل 13 مهمة مجدولة، كل واحدة تحت قفل موزّع باسم فريد.
- التوسّع أفقيًا صار آمنًا: أضف نسخًا من `api` بلا خوف من تكرار المهام.

### أرقام مُتحقَّق منها

| المقياس | القيمة |
| --- | --- |
| ملفات TypeScript | ~460 |
| أسطر TypeScript | ~45,000 |
| وحدات | 60 |
| متحكّمات / نقاط نهاية | ~90 / ~476 |
| جداول قاعدة البيانات | 100 |
| ترحيلات | 66+ |
| مهام مجدولة (كلها مقفولة) | 13 |
| ملفات اختبار | ~40 |

---

## 5. المقارنة مع التطبيقات العالمية

| القدرة | Uber / Bolt / Heetch / Yassir | flaminGO الآن |
| --- | --- | --- |
| مسافة وزمن على الطريق الحقيقي | ✅ | ✅ OSRM |
| تسعير ذروة حسب العرض والطلب | ✅ | ✅ |
| مطابقة بالأسرع وصولًا | ✅ | ✅ |
| دفتر مالي مزدوج القيد | ✅ | ✅ (92/100 — من أقوى أجزاء النظام) |
| محفظة + نقد + بطاقة | ✅ | ✅ (البطاقة تنتظر مفاتيحك) |
| صرف أرباح السائقين بدفعات | ✅ | ✅ |
| فواتير PDF | ✅ | ✅ |
| إكرامية | ✅ | ✅ |
| ولاء وإحالات | ✅ | ✅ |
| SOS ومشاركة الرحلة | ✅ | ✅ |
| كشف الانحراف عن المسار | ✅ | ✅ |
| إخفاء أرقام الهواتف | ✅ | ✅ (Twilio جاهز، `direct` مؤقتًا) |
| مفقودات | ✅ | ✅ |
| بريد معاملاتي متعدد اللغات | ✅ | ✅ |
| أرشفة وتقسيم الجداول | ✅ | ✅ |
| **تغطية اختبارات عالية** | ✅ | ❌ ~35% |
| **مراقبة وتنبيه (APM/تتبّع)** | ✅ | ⚠️ ~45% |
| **نسخة قراءة للتقارير** | ✅ | ❌ غير موجودة |
| **رحلات مجدولة/مشتركة/مطارات** | ✅ | ⚠️ جزئي |
| **كشف احتيال متقدّم بالتعلّم الآلي** | ✅ | ❌ قواعد فقط |

**الحكم:** المنطق الأعمالي بلغ مستوى المنافسين. الفارق المتبقّي ليس في «ماذا يفعل الخادم» بل في «كيف نراقبه ونثبت أنه لا ينكسر».

---

## 6. التقييم التفصيلي: قبل وبعد

| المجال | قبل | بعد (تقدير) | ملاحظة |
| --- | --- | --- | --- |
| الأمن | 74 | 92 | حرّاس عالميون + توقيعات webhook + أسرار محذوفة |
| المدفوعات | 55 | 88 | مُحوّلات حقيقية بدل النجاح الكاذب |
| التسعير | 65 | 90 | مسافة حقيقية + ذروة حيّة |
| المطابقة | 72 | 88 | الأسرع وصولًا |
| الأداء | 68 | 82 | تخزين مؤقت + تقسيم جداول |
| القابلية للتوسّع | 66 | 84 | فصل الأدوار + أقفال + أرشفة |
| قاعدة البيانات | 88 | 92 | تقسيم + أرشفة + فهارس |
| الإشعارات | 70 | 88 | بريد بقوالب موصولة بأحداث حقيقية |
| المحفظة والدفتر المالي | 92 | 94 | كان الأقوى وبقي |
| المراقبة | 45 | 55 | **لا يزال الأضعف** |
| الاختبارات | 35 | 45 | **الأضعف الثاني** |
| **الإجمالي** | **71** | **86** | |

---

## 7. هل هو جاهز للبدء؟ الجواب الصريح

**الكود جاهز. البيئة ليست جاهزة بعد.** ما تبقّى ليس برمجة بل تشغيل، وكله بيدك:

### إلزامي قبل أول راكب حقيقي

1. **تدوير كل الأسرار** (كانت في مستودع Git: قاعدة البيانات، JWT، Redis، مفاتيح المزوّدين).
2. `npx prisma generate && npx prisma migrate deploy`
   ⚠️ ترحيل تقسيم جدول التتبّع **يعيد بناء الجدول**: خُذ نسخة احتياطية، نافذة صيانة، PostgreSQL 12+.
3. **تشغيل OSRM** على خريطة الجزائر (`algeria-latest.osm.pbf`) وضبط `maps.provider="osrm"`. بدونه يعود التسعير إلى الخط المستقيم.
4. **بيانات Firebase Admin** — بدونها لا OTP ولا إشعارات.
5. `APP_ROLE=api` على الخادم و`worker` على العامل — لا تشغّل الاثنين بنفس الدور.
6. `npm run build && npm run typecheck:strict` — لا توجد حزم مثبَّتة في بيئتي فلم أستطع تشغيل البناء أو الاختبارات؛ **لم أدّعِ نجاحهما مطلقًا**.
7. خط `NotoNaskhArabic-Regular.ttf` في `assets/fonts` لفواتير PDF العربية.

### إلزامي قبل التوسّع

8. `CALL_MASKING_PROVIDER=direct` للبدء، أو أرقام Twilio + توجيه webhook الصوت.
9. `CHARGILY_SECRET_KEY` + `CHARGILY_MODE=live` + توجيه webhook عند جهوزية الدفع بالبطاقة.
10. `EMAIL_PROVIDER` + `EMAIL_API_KEY` + توثيق نطاق الإرسال (SPF/DKIM) — بدونه يتخطّى النظام البريد بصمت.
11. بيانات البنك (IBAN) للسائقين قبل أول دفعة صرف.
12. `TRIP_ARCHIVE_ENABLED=false` حتى تراجع نتيجة تشغيل تجريبي (`dryRun`)، ثم شغّلها على دور العامل فقط.
13. مراجعة 13 متغيّر بيئة للولاء والإحالات (قيم المكافآت مال حقيقي).

---

## 8. ما تبقّى في الخطة (بترتيب الأولوية)

| الأولوية | البند | الأثر |
| --- | --- | --- |
| **حرجة** | رفع تغطية الاختبارات إلى 70%+ على المسارات المالية والرحلة | بلا هذا كل نشر مغامرة |
| **حرجة** | مراقبة حقيقية: APM، تتبّع موزّع، تنبيهات على معدل الفشل والكمون | اليوم نعرف بالمشكلة من شكوى مستخدم |
| عالية | بريد الترحيب عند التسجيل | مؤجّل بوعي: يلمس مسار الدخول |
| عالية | قراءة أحداث الرحلات المؤرشفة في مسار القراءة العام | حاليًا يقرأها موظّفو الدعم فقط |
| عالية | نسخة قراءة (Read replica) للتقارير | التقارير الثقيلة تنافس الركّاب على القاعدة |
| متوسطة | تقسيم `financial.service.ts` (~2040 سطرًا) إلى أربع خدمات | صيانة وقابلية اختبار |
| متوسطة | مُحوّل Vonage للاتصالات | بديل تفاوضي لـ Twilio |
| منخفضة | كشف احتيال متقدّم، رحلات مشتركة، حزمة المطار | ميزات نمو لا شروط إطلاق |

---

## 9. توصية الإطلاق

ابدأ بـ **إطلاق تجريبي محكوم**: مدينة واحدة، 20–50 سائقًا، نقد ومحفظة فقط، الأرشفة والذروة معطّلتان في البداية، ومراقبة يدوية يومية للدفتر المالي. راقب ثلاثة أرقام: نسبة قبول العروض، فرق السعر المتوقّع عن الفعلي، وتوازن الدفتر المالي. إن استقرّت هذه الثلاثة أسبوعين، شغّل الذروة ثم البطاقة ثم التوسّع.

الخادم الآن يقول الحقيقة عن نفسه: لم يبقَ فيه مسار يُرجع نجاحًا كاذبًا. وهذه — لا عدد الميزات — هي العلامة الأولى للخادم الاحترافي.
